~targu1n-NaturesUnlock
#src/ui/starter-select-ui-handler.ts
+                item: "candy",
+                itemArgs: starterColors[this.lastSpecies.speciesId]
+              });
+            }
            // Targu1n Nature Unlock
            if(!globalScene.mods.hasAllNatures(this.lastSpecies)){
              options.push({
                label: "Unlock Nature",
                handler: () => {
                   ui.setMode(Mode.STARTER_SELECT).then(() => globalScene.mods.showNatureUnlock(ui, this.lastSpecies, candyCount, this, this.pokemonCandyCountText));
                  return true;
                }
              });
            }
