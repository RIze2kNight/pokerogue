~targu1n-IVsUnlock
#src/ui/starter-select-ui-handler.ts
+                item: "candy",
+                itemArgs: starterColors[this.lastSpecies.speciesId]
+              });
+            }
            // Targu1n IV Unlock
            const isAnyIvBelow31 = [0, 1, 2, 3, 4, 5].some((stat) => globalScene.gameData.dexData[this.lastSpecies.speciesId].ivs[stat] < 31);
            if(isAnyIvBelow31){
              options.push({
                label: "Improve IVs",
                handler: () => {
                  ui.setMode(Mode.STARTER_SELECT).then(() => globalScene.mods.showIVsUnlock(ui, this.lastSpecies, candyCount, this, this.pokemonCandyCountText));
                  return true;
                }
              });
            }
