~targu1n-ShinyUnlock
#src/ui/starter-select-ui-handler.ts
+                item: "candy",
+                itemArgs: starterColors[this.lastSpecies.speciesId]
+              });
+            }
            const hasAllVariants = (attr: bigint) => (attr & DexAttr.VARIANT_3) && (attr & DexAttr.VARIANT_2) && (attr & DexAttr.SHINY);
            if(!hasAllVariants(globalScene.gameData.dexData[this.lastSpecies.speciesId].caughtAttr)){
              options.push({
                label: "Unlock Shinies",
                handler: () => {
                  ui.setMode(Mode.STARTER_SELECT).then(() => globalScene.mods.showShiniesUnlock(ui, this.lastSpecies, candyCount, this, this.pokemonCandyCountText));
                  return true;
                }
              });
            }

