~targu1n-IncreaseEggRarity
#src/data/egg.ts
-      this._tier = eggOptions?.tier ?? (Overrides.EGG_TIER_OVERRIDE ?? this.rollEggTier());
      this._tier = eggOptions?.tier ?? (Overrides.EGG_TIER_OVERRIDE ?? this.rollEggTier(globalScene.mods.overrideEggRarityIndex));


#src/data/egg.ts
-  private rollEggTier(): EggTier {
  private rollEggTier(mult?: number): EggTier {



#src/data/egg.ts
-    return tierValue >= GACHA_DEFAULT_COMMON_EGG_THRESHOLD + tierValueOffset ? EggTier.COMMON : tierValue >= GACHA_DEFAULT_RARE_EGG_THRESHOLD + tierValueOffset ? EggTier.RARE : tierValue >= GACHA_DEFAULT_EPIC_EGG_THRESHOLD + tierValueOffset ? EggTier.EPIC : EggTier.LEGENDARY;
    return tierValue >= (GACHA_DEFAULT_COMMON_EGG_THRESHOLD + tierValueOffset)*mult ? EggTier.COMMON : tierValue >= (GACHA_DEFAULT_RARE_EGG_THRESHOLD + tierValueOffset)*mult ? EggTier.RARE : tierValue >= (GACHA_DEFAULT_EPIC_EGG_THRESHOLD + tierValueOffset)*mult ? EggTier.EPIC : EggTier.LEGENDARY;
