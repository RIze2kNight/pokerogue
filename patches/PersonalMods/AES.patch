~rize2knight-AES
#src\system\game-data.ts
-        const encryptedData = AES.encrypt(dataStr, saveKey);
        const encryptedData = dataStr;
        // const encryptedData = AES.encrypt(dataStr, saveKey);

#src\system\game-data.ts
-            let dataStr = AES.decrypt(e.target.result.toString(), saveKey).toString(enc.Utf8);
            var dataStr;
            try{ dataStr = e.target.result.toString(); }
            catch{ dataStr = AES.decrypt(e.target.result.toString(), saveKey).toString(enc.Utf8); }